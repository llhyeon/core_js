// enumerable type -> 열거 가능한
// 찐 상수
// 값을 넣지 않으면 0이 할당된다.
enum Direction {
  UP = 3, // 0
  DOWN, // 1
  LEFT, // 2
  RIGHT, // 3
}

const direction = {
  up: Direction.UP,
  down: Direction.DOWN,
  left: Direction.LEFT,
  right: Direction.RIGHT,
};
